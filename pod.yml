
---
apiVerson: v1
kind: Pod
metadata:
    name: nginx
    labels:
        app: nginx
        tier: frontend
spec:
    containers:
       - name: nginx
         image: nginx               # specify path if needed
       - name: busybox
         image: busybox


      
